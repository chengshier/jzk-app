# 测试与验收记录

## 已完成静态检查

1. 对新增和修改的 Vue 文件提取 `<script>`，使用 `node --check` 检查 JavaScript 语法，全部通过。
2. 对 Vue 模板进行标签配对检查，未发现未闭合或错误嵌套。
3. 检查本轮新增页面内的 `/pages/...` 路由，除原项目历史可选路由 `/pages/service/index` 外，均能找到对应 Vue 文件。
4. 以下原项目检查脚本通过：
   - `check-home-login-compat.js`
   - `check-jk-identity-application-contract.js`
   - `check-mall-health-contract.js`
   - `check-my-page-fusion-contract.js`
   - `check-ui11-detail-home-contract.js`
   - `verify-jk-fund-account-page.js`
   - `verify-template-compile-guards.js`
   - `verify-ui11-checkout-contract.js`

## 原工程已存在的检查阻塞

以下两个检查在原始 `app.zip` 中也无法通过，并非本轮改造引入：

1. `check-theme6-contract.js`
   - 原因：脚本依赖同级 `admin/src/views/design/theme/index.vue`，本次仅收到 App 工程，没有 Admin 工程。
2. `check-ui11-route-contract.js`
   - 原因：脚本要求 `pages.json` 中存在带特定格式的“选择药品”标题；原始工程已不满足该文本检查。

## 仍需真实环境验证

由于本次未提供可运行后端、登录 Token、微信开发者工具和业务测试数据，以下只能标记为待联调：

- 身份申请上传、提交、审核结果真实返回。
- 平台订货付款凭证、付款审核、发货、收货入库。
- 调拨申请、区县代审核、付款确认、拨货、收货。
- 佣金、资金、提现金额与状态。
- 推广二维码真实生成和保存相册权限。
- 小程序真机顶部胶囊、安全区和自定义导航栏位置。
